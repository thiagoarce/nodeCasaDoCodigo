module.exports = function defineWidget(def, renderer) {
    if (def.___) {
        return def;
    }

    if (renderer) {
        return {
            ___: true,
            _isWidget: true,
            renderer: renderer,
            render: renderer.render,
            renderSync: renderer.renderSync,
            template: renderer.template
        };
    } else {
        return { ___: true, _isWidget: true };
    }
};